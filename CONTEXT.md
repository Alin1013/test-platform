# 测试用例生成上下文

本上下文描述 XMind 转功能测试用例的业务语言，帮助上传、审核和用例管理流程使用一致的术语。

## XMind 生成

**生成预览**：根据 XMind 节点树生成、尚未经过用户确认的功能测试用例集合。生成预览不等同于正式用例。
_避免_: 草稿用例、自动入库结果

**正式用例**：用户确认生成预览并保存到测试用例库后的功能测试用例。
_避免_: 生成结果、预览用例

**用例目录**：由 XMind 节点层级形成的模块路径，用于把生成预览归入测试平台中的模块。
_避免_: 文件夹、目录名

**全量成功**：XMind 被拆分的每个功能分组都成功生成并通过结构校验后，才向用户展示生成预览。
_避免_: 部分成功、降级结果

## 用例字段

**功能用例**：描述用户或系统完成一个功能行为及其可验证结果的测试用例，不包含接口自动化配置。
_避免_: 接口用例、UI 自动化用例

**逆向测试点**：验证边界、异常、权限、重复操作或故障处理的测试点，与正向流程共同构成覆盖范围。
_避免_: 负向用例（除非明确指单一输入校验）
